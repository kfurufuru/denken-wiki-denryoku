{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "if ! lsof -Pi :3456 -sTCP:LISTEN -t >/dev/null 2>&1; then npx --yes claude-task-viewer &>/tmp/claude-task-viewer.log & fi"
          }
        ]
      }
    ]
  }
}
